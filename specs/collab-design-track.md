# Collaborative Design Track — David in the loop (2026-07-24)

> David's ruling: the loop handles execution; David is the taste layer for
> geography, interiors, and furnishing. This file is the working contract.

## 1. Island geography — review pack workflow

- Loop builds an **aerial camera mode** in /lab/world (top-down + tilted
  quadrant framings), captures a full **map review pack** (whole island +
  NE/NW/SE/SW quadrants + coast ring), publishes as one review page.
- David replies in chat with imbalance notes ("NE is empty", "shop crowds
  the river") — each note becomes a loop work item.
- **Cliff system implementation waits for this review** — cliffs are
  geography; David's notes decide where verticality goes.

## 2. Interiors — delicate floor-plan sessions (no random props)

Process per building: **purpose first** (what do members DO in the room),
then zones, then sightlines from the door, then furniture from the
shortlist (see §3). Nothing placed without a plan David approved.

- **First session: HQ** — the TSI home base. Loop prepares a session doc:
  candidate floor plans (2-3 sketches) + the purpose questions (lounge?
  meeting table? trophy wall? notice board?). David rules, then it gets built.
- Enterable roadmap (David 2026-07-24): current HQ/Shop/Oracle + **House**
  and **Lighthouse** get interiors. Future: **Museum** and other civic
  buildings later in the game's life (ties to the TSI Art Museum sketch in
  specs/sprint-2026-07-refinement.md).

## 3. Furnishing — catalog first, then rooms

- The dump holds **10,293 Ftr\* furniture pieces**. Loop builds a
  **browsable furniture catalog**: preview renders by category (the icon
  harness pattern — stage + batch screenshots), published in browsable
  pages, batched across wakes (indoor living categories first).
- David **shortlists** pieces/sets he loves; rooms are furnished ONLY from
  the shortlist. The shortlist lives in this file once it exists.

## Current sequence (2026-07-24)

1. QA Wave 29 on `restart/art-cohesion-v2` (pre-merge gate — already ruled)
2. Aerial mode + geography review pack → David's notes
3. Furniture catalog batches (parallel with 2's review time)
4. HQ floor-plan session doc → David rules → build HQ interior properly
5. Geography fixes from David's notes → then cliff system
6. Launch buildables once specs/rulings-2026-07-24-launch.md is ruled
